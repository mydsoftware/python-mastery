name: Validate Python Mastery Dataset

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  validate-dataset:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Validate exercises
        run: node scripts/validate-dataset.mjs
